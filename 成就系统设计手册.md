# 仙喵五子棋 · 成就系统设计手册

> 状态：**待实现**。本文档为实施依据，按此清单开发即可，无需再讨论。
> 已拍板决策（2026-09-03）：奖励**纯仙喵币**；入口放**主菜单**（对局中达成只在结束时提示）；**只做永久成就**（无每日）。

---

## 一、设计目标

1. 给免费玩家一个稳定的仙喵币来源，推动解锁猫/歌曲（**一次性总量控制在 ~400 币**，配合挑战费消耗，不破坏经济）
2. 给"赢棋"之外增加长线目标（连胜/收集/每只猫的羁绊）
3. 幽默水墨文案风格，强化"和猫下棋"的情感层

## 二、架构（4 个新文件 + 2 处接线）

| 文件 | 职责 |
|---|---|
| `Assets/Scripts/Game/PlayerStats.cs` | 玩家统计累计 + JSON 持久化（同 GameRecordManager 的 records.json 模式，存 `player_stats.json`） |
| `Assets/Scripts/Game/AchievementDef.cs` | ScriptableObject 成就定义（同 CatProfile 惯例） |
| `Assets/Scripts/Game/AchievementsManager.cs` | 监听统计 → 判定达成 → `EconomyManager.AddCoins` 发币 → 事件通知 |
| `Assets/Scripts/UI/AchievementPanel.cs` | 成就弹窗（复用弹窗 + Item 模板惯例，同 SongListPanel） |
| `Assets/Art/UI_Kit/` | 进度条底/已领取角标等小素材（可复用现有） |

**接线点**：
- `GameManagers` 场景节点挂 `PlayerStats` + `AchievementsManager`（同 GameManager 惯例，MonoBehaviour + Instance）
- `MainMenuController`：主菜单加"成就"按钮 → 打开 AchievementPanel

## 三、统计口径（PlayerStats 字段）

| 字段 | 累计时机 | 说明 |
|---|---|---|
| totalGames | GameEnded | **下满 10 手才算有效对局**（防刷） |
| totalWins | GameEnded 且玩家胜 | 同上 |
| currentStreak / maxStreak | GameEnded | 胜 +1，负清零；maxStreak 取历史最大 |
| catGames[猫名] / catWins[猫名] | GameEnded | 按当前出战猫（CatManager.Selected）记 |
| coinEarnedTotal | EconomyManager.AddCoins | 只计收入不计花费 |

- 存档：`Application.persistentDataPath/records/player_stats.json`（JsonUtility）
- ⚠️ JsonUtility 不支持 Dictionary：catGames/catWins 用 `[Serializable] class CatStat { catName; games; wins; }` 的 List 实现
- ⚠️ 发奖顺序：**先 `EconomyManager.AddCoins`，再标记 unlockedIds 并存档**——崩溃窗口最坏是重复领一次（玩家有利）；反过来会永久吞掉玩家奖励
- **重开（TryRestart）未结束的对局不计入**；悔棋不影响统计

## 四、AchievementDef 定义

```csharp
[CreateAssetMenu(menuName = "Wuziqi/Achievement Def")]
public class AchievementDef : ScriptableObject
{
    public string achId;            // 唯一 ID（如 win_20）
    public string displayName;      // 名称（水墨+幽默风）
    [TextArea] public string desc;  // 条件描述
    public AchType type;            // 类型枚举
    public CatProfile targetCat;    // 仅 CatWins 类型用
    public int targetValue;         // 目标值
    public int rewardCoins;         // 奖励仙喵币
    public bool hidden;             // 隐藏成就（达成前不显示条件）
}
public enum AchType { TotalWins, MaxStreak, TotalGames, CatWins, CollectCats, SameCatGames, CoinEarned, LoseStreak }
```

- 达成记录存 PlayerStats JSON 里（`List<string> unlockedIds`），防改表重复领取

## 五、成就清单（17 个，总奖励 ≈ 425 仙喵币）

| # | ID | 名称 | 条件 | 奖励 |
|---|---|---|---|---|
| 1 | win_1 | 初出茅庐 | 首次获胜 | 10 |
| 2 | win_5 | 崭露头角 | 累计胜 5 局 | 15 |
| 3 | win_20 | 小有名气 | 累计胜 20 局 | 20 |
| 4 | win_50 | 棋坛高手 | 累计胜 50 局 | 40 |
| 5 | game_50 | 百炼成钢 | 累计对局 50 局 | 20 |
| 6 | streak_3 | 三连捷 | 连胜 3 局 | 15 |
| 7 | streak_5 | 五连冠 | 连胜 5 局 | 30 |
| 8 | streak_10 | 十连冠 | 连胜 10 局 | 50 |
| 9 | cats_3 | 猫口普查 | 解锁 3 只猫 | 15 |
| 10 | cats_5 | 群猫环绕 | 解锁 5 只猫 | 30 |
| 11 | cats_7 | 全图鉴 | 解锁全部 7 只猫 | 50 |
| 12 | song_3 | 曲库收藏家 | 解锁 3 首歌 | 10 |
| 13 | same_20 | 老友记 | 和同一只猫下满 20 局 | 20 |
| 14 | beat_all | 满门爪牙 | 战胜过所有 7 只猫 | 40 |
| 15 | coin_100 | 第一桶金 | 累计获得 100 仙喵币 | 10 |
| 16 | coin_500 | 富甲一方 | 累计获得 500 仙喵币 | 20 |
| 17 | lose_5 | 喵仙人提款机 | 连败 5 局（隐藏，文案："被喵仙人当成提款机了喵…"） | 10 |

> CollectCats 类型在判定时实时查 `CatManager` 解锁数（遍历 IsUnlocked）；SongList 解锁数同理查歌单数据。

## 六、UI 说明

- **AchievementPanel**：全屏弹窗（同 SettingsPanel 层级），顶部标题"仙喵成就"，下方滚动列表
- **AchievementItem**（prefab 模板）：名称 + 描述 + 进度条（`当前/目标`）+ 右侧状态（已达成→"已领取"；未达成→灰色）
- 隐藏成就未达成时显示"？？？"
- **达成提示**：对局结束时统一 toast（`GameEnded` 后检查新达成，顶部弹出"成就达成：三连捷 +30 仙喵币"，2.5 秒淡出）；主菜单打开面板时可补领
- 主菜单按钮：与开始/退出同排或下方，文字按钮"成就"

## 七、实现顺序（checklist）

- [ ] 1. `PlayerStats.cs`：字段 + JSON 存档 + GameEnded 累计（挂 GameManagers 节点）
- [ ] 2. `AchievementDef.cs` + 编辑器批量生成 17 个 .asset（`Assets/ScriptableObjects/Achievements/`）
- [ ] 3. `AchievementsManager.cs`：判定 + 发币 + unlockedIds 持久化
- [ ] 4. `AchievementPanel.cs` + AchievementItem prefab + 场景接线（主菜单按钮）
- [ ] 5. 结算 toast
- [ ] 6. 编译 + Play Mode 冒烟（模拟 GameEnded 触发成就 + 领币）
- [ ] 7. 打包装机回归

## 八、验收标准

1. 连胜 3 局后 toast 弹出且仙喵币 +15
2. 同一成就不重复发奖（重进游戏/重打一局）
3. 主菜单面板进度条数值与实际战绩一致
4. 卸载重装后统计保留（存档在 persistentDataPath）
5. 未下满 10 手的对局不计入（快速认输 ×N 不刷成就）
